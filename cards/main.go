package main

func main() {
	// card := "Ace of Spades"
	// card = newCard()
	// cards := deck{newCard(), "Ace of Diamonds"}
	// cards := deck{}
	// cards = append(cards, "Six of Spades")

	// for i, card := range cards {
	// 	fmt.Println(i, card)
	// }
	// fmt.Println(cards)
	c1 := newDeck()
	c1.print()
	// cards := newDeck().shuffle()
	// // sCards := cards.shuffle()
	// // // cards.print()
	// hand1, r1 := deal(cards, 5)
	// hand2, r2 := deal(r1, 5)
	// hand3, r3 := deal(r2, 5)
	// hand4, _ := deal(r3, 5)
	// hand1.print()
	// fmt.Println("==============================")
	// hand2.print()
	// fmt.Println("==============================")
	// hand3.print()
	// fmt.Println("==============================")
	// hand4.print()
	// remainingCards.print()
	// saveToFile(hand, "hand")
	// saveToFile(remainingCards, "remainingCards")
	// newFile := newDeckFromFile("hand")
	// newFile.print()

}
